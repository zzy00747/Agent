# Mini Agent TS

这是一个基于 TypeScript 构建的轻量级 AI Agent 命令行工具。它旨在提供多轮对话交互体验，并支持基础的文件操作能力。

## 🛠️ 技术栈

- **语言**: TypeScript
- **运行环境**: Node.js
- **CLI 框架**: Commander.js

## 🚀 开发与调试

常用命令说明：

| 命令            | 作用                                                  |
| :-------------- | :---------------------------------------------------- |
| `npm run build` | 编译 TypeScript 到 `dist` 目录                        |
| `npm run start` | 运行编译后的程序                                      |
| `npm run dev`   | 直接运行 TypeScript（建议开发调试使用，需安装 `tsx`） |
| `npm link`      | 注册 `mini-agent` 命令到系统（只需执行一次）          |
| `mini-agent`    | 全局运行你的 CLI（需要先完成 build 和 link）          |
